package io.codeforall.javatars_filhosdamain.players;


/*public class Slime implements Movable, Interactable {

    private Picture picture;
    private final Position position;
    private static final int HEIGHT = 56;
    private static final int WIDTH = 28;
    private boolean up;
    private boolean down;
    private boolean left;
    private boolean right;

    public Slime() {
        position = new Position(400, 400, WIDTH, HEIGHT);

    }

    public void display() {
        picture = new Picture(position.getX(), position.getY(), "data/sprites/spr_slime_classic.png");
        picture.draw();
    }

    public Picture getSlime() {
        return picture;
    }

    @Override
    public void setKey(int key, boolean state) {
        switch (key) {
            case KeyboardEvent.KEY_UP -> up = state;
            case KeyboardEvent.KEY_DOWN -> down = state;
            case KeyboardEvent.KEY_LEFT -> left = state;
            case KeyboardEvent.KEY_RIGHT -> right = state;
        }
    }

    @Override
    public void translate(double v, double v1) {

    }
}*/
